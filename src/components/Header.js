import React from "react"

function Header () {
    return (
        <div>
            <h1>Employee Directory</h1>
            <p>Click to filter by heading or type a name in the Search box</p>
        </div>
    )
}

export default Header;